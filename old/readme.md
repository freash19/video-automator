# 🎬 HeyGen Automation

Автоматизация создания видео в HeyGen с использованием Playwright.

---

## 📦 Состав проекта

```
heygen_automation/
├── heygen_automation.py    # Основной скрипт
├── config.json             # Конфигурация (редактируемая)
├── scenarios.csv           # Таблица со сценариями
├── venv/                   # Виртуальное окружение Python
├── УСТАНОВКА.md            # Инструкция по первой установке
├── ИНСТРУКЦИЯ.md           # Инструкция по использованию
├── ШПАРГАЛКА.md            # Быстрая шпаргалка
├── start.sh                # Скрипт быстрого запуска (опционально)
└── README.md               # Этот файл
```

---

## 🎯 Для чего этот проект

Автоматизирует:
1. Заполнение сцен текстом из CSV таблицы
2. Удаление пустых сцен
3. Отправку на генерацию
4. Обработку всех частей эпизода последовательно

---

## 🚀 Быстрый старт

### Для тебя (владелец проекта)

```bash
# 1. Запусти Chrome
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 --user-data-dir=~/chrome_automation

# 2. В новом Terminal
cd heygen_automation
source venv/bin/activate
python heygen_automation.py
```

### Для сотрудника

**Дай ему прочитать файлы в таком порядке:**
1. `УСТАНОВКА.md` - первый раз для установки
2. `ИНСТРУКЦИЯ.md` - для работы со скриптом
3. `ШПАРГАЛКА.md` - для быстрого запуска

---

## ⚙️ Конфигурация (config.json)

```json
{
  "csv_file": "scenarios.csv",          // Имя CSV файла
  "max_scenes": 15,                     // Макс. сцен в шаблоне
  "episode_to_process": "ep_1",         // Какой эпизод обрабатывать
  "chrome_profile_path": "~/chrome_automation"  // Путь к профилю Chrome
}
```

**Что можно менять:**
- `csv_file` - если CSV называется по-другому
- `max_scenes` - если в шаблоне не 15 сцен
- `episode_to_process` - для обработки другого эпизода

---

## 📊 Формат CSV таблицы

| Колонка | Описание | Пример |
|---------|----------|--------|
| episode_id | ID эпизода | `ep_1`, `ep_2` |
| part_idx | Номер части эпизода | `1`, `2`, `3` |
| scene_idx | Номер сцены в части | `1`, `2`, `3` |
| speaker | Кто говорит | `A`, `B` |
| text | Текст сцены | `Hello world` |
| title | Название видео | `ep_1_part_1` |
| template_url | Ссылка на шаблон HeyGen | `https://app.heygen.com/...` |

**Важно:**
- `template_url` указывается **только в первой строке каждого эпизода**
- Остальные строки этого эпизода должны иметь пустую ячейку в `template_url`

**Пример:**
```csv
episode_id,part_idx,scene_idx,speaker,text,title,template_url
ep_1,1,1,A,Привет мир,ep_1_part_1,https://app.heygen.com/create-v4/draft?template_id=xxx
ep_1,1,2,B,Как дела?,ep_1_part_1,
ep_1,2,1,A,Отлично!,ep_1_part_2,
```

---

## 🔧 Технические детали

### Технологии
- Python 3.12+
- Playwright (для автоматизации браузера)
- Pandas (для работы с CSV)
- Chrome DevTools Protocol (CDP)

### Алгоритм работы

1. Подключение к Chrome через CDP (порт 9222)
2. Чтение данных из CSV
3. Для каждой части эпизода:
   - Открытие страницы шаблона
   - Ожидание загрузки элементов
   - Заполнение сцен текстом (через `keyboard.insert_text`)
   - Удаление пустых сцен (выше количества заполненных)
   - Нажатие кнопки "Сгенерировать"
   - Заполнение финального окна (название из `title`)
   - Нажатие "Отправить"
   - Ожидание редиректа на `/projects`
   - Закрытие вкладки
4. Пауза 5 секунд между частями

### Стратегии поиска элементов

- Текстовые поля: `span[data-node-view-content-react]:has-text("text_X")`
- Кнопка меню: `button:has(iconpark-icon[name="more-level"])`
- Удаление: `div[role="menuitem"]:has(iconpark-icon[name="delete"])`
- Кнопка "Сгенерировать": `button:has-text("Сгенерировать")`
- Поле названия: `input[placeholder="Без названия — видео"]`
- Кнопка "Отправить": `button:has-text("Отправить")`

### Эмуляция человека

- Случайные задержки между действиями (0.1-1.0 сек)
- Использование `keyboard.insert_text` для быстрой вставки
- Ожидание загрузки элементов через `wait_for_selector`
- Скроллинг к элементам через `scroll_into_view_if_needed`

---

## 🐛 Отладка

### Включить подробный вывод

В скрипте можно добавить:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Посмотреть, что в CSV

```bash
cat scenarios.csv | head -10
```

### Проверить конфиг

```bash
cat config.json | python -m json.tool
```

### Проверить подключение к Chrome

```bash
curl http://localhost:9222/json/version
```

Должен вернуть JSON с информацией о браузере.

---

## 📈 Возможные улучшения

- [ ] Параллельная обработка нескольких частей
- [ ] Обработка нескольких эпизодов одной командой
- [ ] GUI интерфейс для сотрудников
- [ ] Логирование в файл
- [ ] Автоматическая проверка шаблона на количество сцен
- [ ] Поддержка Google Sheets напрямую
- [ ] Уведомления в Telegram/Slack о завершении

---

## 🔒 Безопасность

- Chrome профиль изолирован в `~/chrome_automation`
- Не хранит пароли в коде
- Использует существующую авторизацию в Chrome
- Не передает данные третьим лицам

---

## 📝 Changelog

### v1.0.0 (текущая версия)
- ✅ Автоматическое заполнение сцен
- ✅ Удаление пустых сцен
- ✅ Обработка всех частей эпизода
- ✅ Конфигурация через JSON
- ✅ Подробная документация

---

## 🤝 Поддержка

Если нужна помощь:
1. Проверь `УСТАНОВКА.md` и `ИНСТРУКЦИЯ.md`
2. Посмотри раздел "Частые проблемы"
3. Напиши мне с описанием проблемы и скриншотом

---

## 📄 Лицензия

Проприетарный проект. Использование только внутри компании.

---

**Создано с ❤️ для автоматизации рутины**
